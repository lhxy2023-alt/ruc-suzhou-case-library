# 2026-03-24

- 用户明确更新偏好：以后写代码或给命令时，不需要在 `openclaw` 前面加 `pnpm`；应优先直接使用全局 `openclaw` 命令。
- 用户要求在当前工作区内从零重做“人大中法升学案例库”前台原型，明确指出不要沿用旧 `project-library/` 的轻量 MVP 思路，只可把它作为历史参考。
- 已新建独立目录 `ruc-suzhou-case-library/`，采用原生模块化前端结构重做移动端原型，包含案例列表页、案例详情页、资讯页、筛选底部面板、Offer 预览占位与资料领取 CTA；整体视觉改为偏红色教育服务品牌风格，而非旧版蓝色轻量风格。
- 已按未来 Feishu Base 对接方向重构 mock 数据层，拆分为 `cases / mentors / articles / documents / filters / fieldMap`，录入 12 条案例（覆盖 25Fall 与 26Fall）、3 位导师、5 条资讯、3 份资料，并明确 direct fields 与 derived fields 的代码边界。
- 已完成代码层自查：修复默认搜索词“中国人民大学（苏州）”与数据中“苏州校区”不匹配导致首页空列表的问题；用 Node 校验了模块语法、案例数量、届别覆盖、筛选覆盖和详情页导师/资料关联。
- 用户新增长期要求：以后“美股早消息”这类早报在定时任务完成并写入飞书文档后，需要再通过飞书主动发给用户做提醒/通知。
- 用户明确要求“人大中法升学案例库”停止沿用旧 MVP 字段假设，必须改为严格建立在 `feishu-bitable-sync/config.json` 与 `feishu-bitable-sync/data/rebuilt-offers.json` 的真实字段和真实 offer 记录之上；本次已将 `ruc-suzhou-case-library/` 的前台案例数据层重建为 25Fall 44 条 + 26Fall 23 条单条真实 offer 记录，移除路径、导师、时间线、营销文案等旧 demo 主字段，并新增 `ruc-suzhou-case-library/FIELD_MAPPING.md` 记录前台字段、后台字段和派生字段边界。
- 用户对后续小程序前台修改提出了成体系的新要求，并要求在其说完全部问题前不要立刻让 Codex 修改；当前已完成一轮需求归纳并发给 Codex，重点包括：修复搜索框中文输入与卡顿；首页删除内部解释性文案并将“资讯”改为“专访”；删除首页冗余说明/统计模块；筛选区改为更贴近业务的结构（去掉本科学校，新增“院校专业”二级菜单，并把录取院校筛选改为国家/地区）；列表卡片重做为更紧凑的小程序案例卡（标题形如“xx大学xx专业offer”，只保留申请季、学生背景、语言/标化，不显示上传时间与实习科研）；详情页大改为参考图式的录取详情结构（保留学生姓名+展示标签、录取学校/专业、本科专业、绩点或均分、语言与标化、实习、科研、备注、申请轮次、录取时间，且空字段不展示），移除“服务导师”并改为“学生名片”逻辑；底部联系入口改成页面下端常驻小浮窗而非页尾大块 CTA。
- 用户明确补充：前台列表页不展示“备注”，但详情页如果备注字段有值则可以展示；“学生名片”逻辑按学生维度控制，即若某学生愿意开放咨询名片，则该学生所有 offer 详情页都应展示该名片。
- 针对上述结构化修改，Codex 已完成新一轮提交，最新 commit 为 `4edceeb feat: refine case library frontend experience`。用户随后要求重启 OpenClaw gateway；已执行 `openclaw gateway restart`，返回“restart scheduled, gateway will restart momentarily”。
- 用户要求：记住我们目前正在推进的“小程序工作”，因为其将重新启动对话继续推进；后续新会话默认应延续当前 `ruc-suzhou-case-library/` 项目的上下文，以及上述真实 Base 驱动、筛选重构、卡片重做、详情页大改、学生名片、底部浮窗等需求。
- 今日围绕公司 AI 员工体系做了多 agent 设计：第一阶段确定先只设计 3 个核心 agent——总控 Agent、获客运营 Agent、销售转化 Agent；其中总控负责需求拆解、优先级排序、任务路由与结果汇总，获客运营负责内容策划/渠道打法/引流设计，销售转化负责微信承接/客户分层/话术/异议处理/跟进建议。
- 今日进一步确定飞书落地方式：长期目标是 1 个 agent 对应 1 个飞书机器人；正式结构为“乐湖总控 / 乐湖增长 / 乐湖成交”三个机器人分别对应总控、获客运营、销售转化三个岗位，避免一个机器人混用多个角色导致上下文污染与职责混乱。
- 结合当前 OpenClaw 配置与已接入状态，已确认现有这只已接入 OpenClaw 的飞书机器人最适合先作为“总控机器人”过渡使用；原因是当前 Feishu default account 已绑定 `main` agent，天然更像统一入口/总控入口。后续若扩展多 agent，再新增“乐湖增长”“乐湖成交”两个飞书机器人并分别做独立 account/binding 映射。
- 今日已为现有飞书机器人准备总控 Agent prompt，并写入 `~/.openclaw/openclaw.json` 的 `channels.feishu.systemPrompt`；同时排查发现飞书直聊会话存在大量旧上下文，会压制新总控 prompt 的表现，因此已删除对应直聊 session 本地文件并让用户在飞书中通过 `new` 重新开启会话，以便让该机器人按“总控助理”定位重新起效。
