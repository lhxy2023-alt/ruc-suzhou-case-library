- 今天排查并确认了 OpenClaw 当前版本中 memory_search 的真实限制：聊天模型可以走 `openai-codex/gpt-5.4` OAuth，但 memory_search 的 embeddings 链路不能复用这条 OAuth 认证。
- 源码级结论已复核：memory_search 支持的 provider 为 `openai` / `local` / `gemini` / `voyage` / `mistral` / `ollama` / `auto`，不包含 `openai-codex`；默认 OpenAI embedding model 为 `text-embedding-3-small`；配置显式依赖 `agents.defaults.memorySearch.remote.apiKey`。
- 已按用户指定方案把 memory_search 切换为 OpenAI + API key：
  - `agents.defaults.memorySearch.provider = openai`
  - `agents.defaults.memorySearch.remote.apiKey` 已写入配置
- 验证结果：`openclaw memory status --deep` 显示 `Embeddings: ready`，`memory_search` 工具与 `openclaw memory search` CLI 均已恢复正常返回结果。
- 过程里发生一次 git 操作失误：我错误执行了 `git add . && git commit`，把大量不应纳入本次提交的文件一起提交；随后已按用户要求先做止血，使用 `git reset --mixed HEAD~1` 撤销了该误提交。
- 后续稳定结论：如果未来再次出现 memory_search 不稳定，不要再假设它能直接复用 openai-codex OAuth；应优先检查 embedding provider 是否已单独配置。
- 今日对 workspace 目录进行归类后，已确认：`ruc-suzhou-case-library` 是当前重要正式业务项目，暂时不要为整理目录而贸然迁移；`feishu-bitable-sync` 是重要的 agent 写飞书多维表格 / 同步 Feishu Base 的工具型工程，应作为跨 agent 的基础能力来维护。
- 用户新增明确要求：未来无论开启任何新会话、使用任何 agent，默认都应能使用 `feishu-bitable-sync` 这套写飞书多维表格的工具能力；后续即便新增 AI 员工，也应默认具备这项能力。